# Quiz System Architecture

## Component Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│                      Profile.jsx                             │
│  (Patient Profile Page with Tabs)                           │
│                                                              │
│  ┌────────┬─────────┬──────────────┬──────────┐            │
│  │Overview│Sessions │ Assessments  │ Settings │            │
│  └────────┴─────────┴──────────────┴──────────┘            │
│                           │                                  │
│                           │ (Assessments Tab)                │
│              ┌────────────┴────────────┐                    │
│              │                         │                     │
│     ┌────────▼──────────┐   ┌─────────▼─────────┐          │
│     │   List View       │   │   Taking View      │          │
│     │                   │   │                    │          │
│     │  ┌─────────────┐  │   │  ┌──────────────┐ │          │
│     │  │ QuizCard    │  │   │  │ QuizTaking   │ │          │
│     │  │ (PHQ-9)     │  │   │  │              │ │          │
│     │  └─────────────┘  │   │  │ - Questions  │ │          │
│     │  ┌─────────────┐  │   │  │ - Navigation │ │          │
│     │  │ QuizCard    │  │   │  │ - Progress   │ │          │
│     │  │ (IPIP-50)   │  │   │  └──────────────┘ │          │
│     │  └─────────────┘  │   └───────────────────┘          │
│     │  ┌─────────────┐  │            │                      │
│     │  │ QuizCard    │  │            │                      │
│     │  │ (IPIP-120)  │  │   ┌────────▼─────────┐           │
│     │  └─────────────┘  │   │  Results View    │           │
│     └───────────────────┘   │                  │           │
│                              │ ┌──────────────┐│           │
│                              │ │ QuizResults  ││           │
│                              │ │              ││           │
│                              │ │ - Scores     ││           │
│                              │ │ - Charts     ││           │
│                              │ │ - Actions    ││           │
│                              │ └──────────────┘│           │
│                              └─────────────────┘           │
└──────────────────────────────────────────────────────────────┘
```

## Data Flow

```
┌──────────────┐
│   User       │
│   Action     │
└──────┬───────┘
       │
       │ 1. Click "Start Assessment"
       ▼
┌──────────────┐
│ handleStart  │────────┐
│   Quiz()     │        │
└──────────────┘        │
       │                │
       │ 2. Call API    │
       ▼                │
┌──────────────┐        │
│ quizService  │        │
│  .start()    │        │
└──────┬───────┘        │
       │                │
       │ 3. POST /api/quiz/{type}/start
       ▼                │
┌──────────────┐        │
│   Backend    │        │
│   Creates    │        │
│   Session    │        │
└──────┬───────┘        │
       │                │
       │ 4. Session Cookie
       ▼                │
┌──────────────┐        │
│ quizService  │        │
│.getQuestions │        │
└──────┬───────┘        │
       │                │
       │ 5. GET /api/quiz/{type}/questions
       ▼                │
┌──────────────┐        │
│   Backend    │        │
│   Returns    │        │
│  Questions   │        │
└──────┬───────┘        │
       │                │
       │ 6. Questions Array
       ▼                │
┌──────────────┐        │
│ QuizTaking   │◄───────┘
│  Component   │
└──────┬───────┘
       │
       │ User answers questions
       │ ┌────────────────┐
       │ │ For each answer│
       │ └────────┬───────┘
       │          │
       ▼          ▼
┌──────────────────────┐
│ handleAnswerSelect() │
└──────────┬───────────┘
           │
           │ POST /api/quiz/{type}/submit-question
           ▼
    ┌──────────────┐
    │   Backend    │
    │   Saves      │
    │   Answer     │
    └──────────────┘
           │
           │ Continue until all answered
           │
           ▼
    ┌──────────────┐
    │ handleSubmit │
    │   Quiz()     │
    └──────┬───────┘
           │
           │ POST /api/quiz/{type}/submit-quiz
           ▼
    ┌──────────────┐
    │   Backend    │
    │  Calculates  │
    │   Scores     │
    └──────┬───────┘
           │
           │ Returns results object
           ▼
    ┌──────────────┐
    │handleComplete│
    └──────┬───────┘
           │
           │ Store results
           ▼
    ┌──────────────┐
    │ QuizResults  │
    │  Component   │
    └──────────────┘
           │
           │ Display scores
           ▼
        ┌──────┐
        │ User │
        └──────┘
```

## State Management

```
Profile Component State
├── activeQuiz: 'phq9' | 'ipip50' | 'ipip120' | null
├── quizView: 'list' | 'taking' | 'results'
├── quizResults: {
│   phq9?: { score: number, ... },
│   ipip50?: { traits: {...}, ... },
│   ipip120?: { traits: {...}, ... }
│   }
└── completedQuizzes: {
    phq9: boolean,
    ipip50: boolean,
    ipip120: boolean
    }

QuizTaking Component State
├── questions: Question[]
├── currentIndex: number
├── answers: { [questionId]: answer }
├── loading: boolean
├── error: string | null
├── submitting: boolean
└── progress: { current: number, total: number }
```

## API Integration

```
quizService Structure
├── ipip120
│   ├── start()
│   ├── getQuestions()
│   ├── submitQuestion()
│   ├── submitQuiz()
│   ├── getResponses()
│   └── getProgress()
├── ipip50
│   ├── start()
│   ├── getQuestions()
│   ├── submitQuestion()
│   ├── submitQuiz()
│   ├── getProgress()
│   ├── getResponses()
│   └── reset()
├── phq9
│   ├── start()
│   ├── getQuestions()
│   ├── submitQuestion()
│   ├── submitQuiz()
│   ├── getProgress()
│   ├── getResponses()
│   └── reset()
└── Helper Functions
    ├── getQuizName()
    ├── getQuizDescription()
    ├── getQuizDuration()
    └── getTotalQuestions()

All methods use apiClient for HTTP requests
└── apiClient
    └── axios instance with base URL and interceptors
```

## Backend Endpoints

```
PHQ-9 Routes (/api/quiz/phq9)
├── POST   /start              → Create session
├── GET    /questions          → Fetch questions
├── POST   /submit-question    → Save answer
├── POST   /submit-quiz        → Calculate score
├── GET    /progress           → Check completion
├── GET    /responses          → Get answers
└── DELETE /reset              → Clear session

IPIP-50 Routes (/api/quiz/ipip50)
├── POST   /start              → Create session
├── GET    /questions          → Fetch questions
├── POST   /submit-question    → Save answer
├── POST   /submit-quiz        → Calculate traits
├── GET    /progress           → Check completion
├── GET    /responses          → Get answers
└── DELETE /reset              → Clear session

IPIP-120 Routes (/api/quiz/ipip120)
├── POST   /start              → Create session
├── GET    /questions          → Fetch questions
├── POST   /submit-question    → Save answer
├── POST   /submit-quiz        → Calculate traits
├── GET    /responses          → Get answers
└── GET    /progress           → Check completion
```

## User Interaction Flow

```
┌─────────────────────────────────────────────────┐
│  1. USER LANDS ON ASSESSMENTS TAB               │
│                                                  │
│  Sees 3 quiz cards:                             │
│  • PHQ-9 Depression Screening                   │
│  • IPIP-50 Personality                          │
│  • IPIP-120 Comprehensive                       │
└─────────────────┬───────────────────────────────┘
                  │
                  │ User clicks "Start Assessment"
                  ▼
┌─────────────────────────────────────────────────┐
│  2. QUIZ SESSION STARTS                         │
│                                                  │
│  • Backend creates session                      │
│  • Cookie stored for auth                       │
│  • Questions load                               │
│  • View changes to "taking"                     │
└─────────────────┬───────────────────────────────┘
                  │
                  │ Questions displayed
                  ▼
┌─────────────────────────────────────────────────┐
│  3. USER ANSWERS QUESTIONS                      │
│                                                  │
│  For each question:                             │
│  • User selects answer                          │
│  • Answer auto-saves to backend                 │
│  • Progress bar updates                         │
│  • Checkmark appears                            │
│  • User clicks "Next"                           │
└─────────────────┬───────────────────────────────┘
                  │
                  │ All questions answered
                  ▼
┌─────────────────────────────────────────────────┐
│  4. USER SUBMITS QUIZ                           │
│                                                  │
│  • User clicks "Submit Quiz"                    │
│  • Confirmation if incomplete                   │
│  • Backend processes answers                    │
│  • Scores calculated                            │
└─────────────────┬───────────────────────────────┘
                  │
                  │ Results ready
                  ▼
┌─────────────────────────────────────────────────┐
│  5. RESULTS DISPLAYED                           │
│                                                  │
│  PHQ-9:                                         │
│  • Score (0-27)                                 │
│  • Severity level                               │
│  • Color coding                                 │
│  • Recommendations                              │
│                                                  │
│  IPIP:                                          │
│  • 5 trait scores                               │
│  • Progress bars                                │
│  • Interpretations                              │
│  • Summary                                      │
└─────────────────┬───────────────────────────────┘
                  │
                  │ User chooses action
                  ▼
┌─────────────────────────────────────────────────┐
│  6. POST-RESULTS ACTIONS                        │
│                                                  │
│  Options:                                       │
│  • "Back to Assessments" → Return to list      │
│  • "Retake Assessment" → Start fresh           │
│                                                  │
│  If retake:                                     │
│  • Session resets                               │
│  • New session starts                           │
│  • Returns to step 3                            │
│                                                  │
│  If back:                                       │
│  • View changes to "list"                       │
│  • Card shows "View Results" button             │
│  • Can start different quiz                     │
└─────────────────────────────────────────────────┘
```

## Session Management

```
Cookie-Based Authentication
┌────────────────────────────────────────┐
│  1. POST /start                        │
│     ↓                                  │
│  Backend creates session               │
│     ↓                                  │
│  Set-Cookie: JSESSIONID=xyz123         │
│     ↓                                  │
│  Browser stores cookie                 │
└────────────────┬───────────────────────┘
                 │
                 │ Subsequent requests
                 ▼
┌────────────────────────────────────────┐
│  GET /questions                        │
│  Cookie: JSESSIONID=xyz123             │
│     ↓                                  │
│  Backend validates session             │
│     ↓                                  │
│  Returns user's questions              │
└────────────────┬───────────────────────┘
                 │
                 │ For each answer
                 ▼
┌────────────────────────────────────────┐
│  POST /submit-question                 │
│  Cookie: JSESSIONID=xyz123             │
│     ↓                                  │
│  Backend links to session              │
│     ↓                                  │
│  Stores answer                         │
└────────────────┬───────────────────────┘
                 │
                 │ Final submission
                 ▼
┌────────────────────────────────────────┐
│  POST /submit-quiz                     │
│  Cookie: JSESSIONID=xyz123             │
│     ↓                                  │
│  Backend retrieves all answers         │
│     ↓                                  │
│  Calculates scores                     │
│     ↓                                  │
│  Returns results                       │
│     ↓                                  │
│  Session can be cleared/reset          │
└────────────────────────────────────────┘
```

## Error Handling

```
Error Scenarios
├── Network Failure
│   ├── Detection: catch block in service methods
│   ├── Display: Error toast message
│   └── Recovery: Retry button, preserve local state
│
├── Session Expired
│   ├── Detection: 401/403 response
│   ├── Display: "Session expired" message
│   └── Recovery: Restart quiz
│
├── Invalid Answer
│   ├── Detection: 400 response
│   ├── Display: Validation error
│   └── Recovery: Fix and resubmit
│
├── Server Error
│   ├── Detection: 500 response
│   ├── Display: Generic error message
│   └── Recovery: Try again later
│
└── Loading Failure
    ├── Detection: Questions array empty
    ├── Display: "No questions available"
    └── Recovery: Go back or retry
```

## Responsive Design

```
Desktop (≥768px)
┌────────────────────────────────────────┐
│  Profile Header                        │
├────────────────────────────────────────┤
│  [Overview] [Sessions] [Assessments]   │
├────────────────────────────────────────┤
│                                        │
│  ┌────────────┐  ┌────────────┐      │
│  │ Quiz Card  │  │ Quiz Card  │      │
│  │  PHQ-9     │  │  IPIP-50   │      │
│  └────────────┘  └────────────┘      │
│                                        │
│  ┌────────────┐                       │
│  │ Quiz Card  │                       │
│  │ IPIP-120   │                       │
│  └────────────┘                       │
└────────────────────────────────────────┘

Mobile (<768px)
┌──────────────────┐
│ Profile Header   │
├──────────────────┤
│ [Tabs Scroll]    │
├──────────────────┤
│                  │
│ ┌──────────────┐│
│ │ Quiz Card    ││
│ │   PHQ-9      ││
│ └──────────────┘│
│                  │
│ ┌──────────────┐│
│ │ Quiz Card    ││
│ │  IPIP-50     ││
│ └──────────────┘│
│                  │
│ ┌──────────────┐│
│ │ Quiz Card    ││
│ │ IPIP-120     ││
│ └──────────────┘│
└──────────────────┘
```

This architecture provides a complete, production-ready quiz assessment system!
